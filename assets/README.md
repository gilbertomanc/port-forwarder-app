# assets

- `icon.ico`: pendiente de generar (PyInstaller/Inno Setup, fase 4).
- `tray_icons/`: pendiente (estados ok/atencion/error para pystray).
- El tray actual genera el icono por código (PIL) si la GUI está instalada.
